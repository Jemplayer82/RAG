---
phase: 2
plan: "02-01"
title: Quality Fixes
wave: 1
status: in_progress
requirements: [RAG-01, RAG-02, SEC-06, DOCS-01]
---

# Phase 2 / Plan 02-01 — Quality Fixes

**Goal:** Retrieval ranking sound, injection/XSS closed, lower-severity issues + docs cleaned, then rebuild. Local-first drafts (`gemma4:e4b`), orchestrator-gated, atomic commits.

## Tasks
- [ ] Q1 `RAG-01` — `rag_async.py`: normalize BM25 (and semantic) scores to [0,1] before the 0.6/0.4 fusion so unbounded BM25 doesn't dominate.
- [ ] Q2 `RAG-02` — `rag_async.py`: thread `chat_history` into the prompt (last few turns) instead of ignoring it.
- [ ] Q3 `RAG-02` — `rag_async.py`: drop dead metadata keys (`case_name`, `doc_id`) never set anywhere.
- [ ] Q4 `SEC-06` — `templates/chat.html`: escape `citation`/`doc_type`/href in the sources panel innerHTML; fix `__x__`→bold markdown token.
- [ ] Q5 `SEC-06` — `app_fastapi.py`: build the Ollama-pull error channel with `json.dumps`, not hand-interpolated f-string JSON.
- [ ] Q6 — `app_fastapi.py`: download endpoint — validate/escape the open-redirect to `doc.url` (only http/https external).
- [ ] Q7 — `app_fastapi.py`: migrate `@app.on_event` → FastAPI lifespan handler (deprecation).
- [ ] Q8 — `config.py`: align upload dir (`raw/uploads` created vs `uploads` used).
- [ ] Q9 — `ingest.py`: chunk-overlap edge (oversized-paragraph carry), crawl word-count note.
- [ ] Q10 — `Dockerfile`: don't silently swallow the playwright/chromium install failure (log it).
- [ ] Q11 `DOCS-01` — `CLAUDE.md`/`QUICK_START.md`: correct to the FastAPI/Qdrant reality; document new `ADMIN_USERNAME` env.

## Verification
- Keyword-heavy and semantic queries both return sensible top-k (Q1) — checked in Phase 3.
- Crafted Ollama model name can't break the pull error JSON (Q5); chat output with HTML-ish doc titles renders escaped (Q4).
- Docs match reality (Q11). Stack rebuilds healthy.
