# MCP server deps — local-only, kept OUT of the Docker image.
# Install on the machine that hosts the MCP (Claude Desktop / Claude Code):
#     pip install -r requirements-mcp.txt
# Requires Python 3.10+.

mcp>=1.2.0
httpx>=0.27
